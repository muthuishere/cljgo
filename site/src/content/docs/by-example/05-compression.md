---
title: "Compression"
sidebar:
  label: "5. Compression"
  order: 5
---

<!-- GENERATED by spikes/s67-docs-by-example (ADR 0104) from conformance/tests/cljg-compress-roundtrip.clj — do not edit.
     The example below is a frozen conformance test: CI runs it in BOTH the
     interpreter and a compiled binary on every commit, so it cannot rot. -->

gzip, deflate and zlib are built in — compress a value, get it back, stream it. No dependency, works in a 6 MB static binary.

cljg.compress (ADR 0103 wave 1, spike s61): gzip / deflate / zlib round-trips over Go's stdlib compress/*. Freezes ROUND-TRIP EQUALITY and decompressed CONTENT only — never raw compressed bytes (gzip headers embed an OS byte and mtime, so compressed output is not stable across platforms). :string}), an explicit :level, the empty string, the streaming decompress-on-read wrappers composing with cljg.stream (read-all over a gunzip-stream of a byte-array source), and that corrupt input / a bad :level throw instead of returning garbage.

so this file cannot run through the `clojure` CLI verbatim. The codec semantics are RFC 1952/1951/1950 as implemented by Go's compress/gzip, compress/flate and compress/zlib (round-trip identity proven in spike

```clojure
(require '[cljg.compress :as z])
(require '[cljg.stream :as st])
(let [s "the quick brown fox jumps over the lazy dog — cljg.compress 0123456789"]
  [(= s (z/gunzip (z/gzip s) {:as :string}))
   (= s (z/inflate (z/deflate s) {:as :string}))
   (= s (z/zlib-decompress (z/zlib-compress s) {:as :string}))
   (= s (z/gunzip (z/gzip s {:level 9}) {:as :string}))
   (= "" (z/gunzip (z/gzip "") {:as :string}))
   (= s (st/read-all (z/gunzip-stream (z/gzip s))))
   (= s (st/read-all (z/inflate-stream (z/deflate s))))
   (= s (st/read-all (z/zlib-decompress-stream (z/zlib-compress s))))
   (try (z/gunzip "not gzip data") (catch Throwable e :threw))
   (try (z/gzip s {:level 42}) (catch Throwable e :threw))])
```

Output:

```clojure
[true true true true true true true true :threw :threw]
```

Run it yourself — save as `ex.clj` and:

```bash
cljgo run ex.clj      # interpreted, instant
cljgo build           # or ship it as a static binary
```
