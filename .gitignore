# reference clones — read-only study material, never committed
refs/

# emitted output (user-owned generated Go modules)
gen/

# go
*.test
*.out

# goreleaser build output
dist/

# editor/os
.DS_Store
.idea/
.vscode/

# compiled spike binaries (rebuild with go build)
spikes/**/*.bin
s3repl

# local build artifact
/cljgo

# spike modules are self-contained
spikes/s11-result-rep/cljgo-spike*

# overnight-loop runtime lock (not project content)
scheduled_tasks.lock
.claude/scheduled_tasks.lock

# agent worktrees (runtime, branches merged into main)
.claude/worktrees/
