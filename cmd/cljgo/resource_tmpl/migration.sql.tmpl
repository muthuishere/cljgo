-- {{.Timestamp}}_create_{{.Table}}.sql
-- generated by `{{.Command}}`
-- bri.db versioned migration (ADR 0072); sqlite by default (ADR 0057).
CREATE TABLE {{.Table}} (
  id INTEGER PRIMARY KEY AUTOINCREMENT{{range .Fields}},
  {{.SQLDecl}}{{end}}
);
{{range .Fields}}{{if .IsRef}}CREATE INDEX idx_{{$.Table}}_{{.Column}} ON {{$.Table}} ({{.Column}});
{{end}}{{end}}