
=== CASE 4 — TRANSITIVE DISCOVERY WITHOUT RUNNING DEP BUILD CODE ===

fixture root: /var/folders/cb/fw9_2_fd39n1jn_xm5r7q1dh0000gn/T/s26-manifest-2246611154

-- resolve: read build.lock.edn (S33 schema); never evaluate a build fn --
   libhttp  -> github.com/google/go-cmp v0.6.0
   libuuid  -> github.com/google/uuid v1.6.0
   libuuid  -> github.com/google/go-cmp v0.7.0

-- policy A: naive append (today) --
   github.com/google/go-cmp@v0.6.0, github.com/google/uuid@v1.6.0, github.com/google/go-cmp@v0.7.0
   => duplicate path in go.mod; `go mod tidy` silently picks the higher.

-- policy B: MVS in cljgo (pick higher, record provenance) --
   github.com/google/go-cmp@v0.7.0, github.com/google/uuid@v1.6.0
   note: github.com/google/go-cmp: v0.7.0 (from libuuid) upgraded over v0.6.0 (from libhttp)

-- policy C: hard error on any disagreement --
   cljgo build would fail with:
   conflicting go-require for github.com/google/go-cmp
     libhttp pins v0.6.0
     libuuid pins v0.7.0
   resolve it in build.cljgo with an explicit (go-require ...) override
