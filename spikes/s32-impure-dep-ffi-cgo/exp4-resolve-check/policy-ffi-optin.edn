;; Option (b) from ADR 0048 decision 6: the consumer EXPLICITLY opts in to
;; :ffi, and does not require cross-compilation. fastjson-cgo is dropped.
{:project "consumer-app"
 :allow   [:ffi]
 :require-cross-compile false
 :deps ["sqlite-lite"]}
